<header>
	<h1>Create a Campus</h1>
</header>

<%= render 'shared/errors', object: @campus %>
<%= render partial: 'form', locals: {send_remote: false} %>

<footer>
	<%= link_to 'Cancel', campuses_url %>
</footer>
