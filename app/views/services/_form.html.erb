<%= form_for @service do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %><br />

	<%= f.label :description %>
	<%= f.text_field :description %><br />

	<%= f.submit %> <%= link_to 'Cancel', services_url %>
<% end %>