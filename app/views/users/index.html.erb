<header>
  <h1><%= pluralize(@users.count, "User", "Users") %></h1>
</header>
<article>
  <ul>
  <% @users.each do |user| %>
    <li><%= link_to user.name, user_path(user) %></li>
  <% end %>
</ul>
</article>
<footer>
  <%= link_to "Create User", new_user_path  %>
</footer>